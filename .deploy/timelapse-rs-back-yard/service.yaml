apiVersion: v1
kind: Service
metadata:
  name: timelapse-rs-back-yard
  namespace: home-sensors
spec:
  type: LoadBalancer
  selector:
    app: timelapse-rs-back-yard
  ports:
    - name: http
      protocol: TCP
      port: 9100
      targetPort: 9090
